export const createData = (
  brand_name: string,
  purpose: number,
  product_type: number,
  manufacturer_name: number,
  id: number
) => {
  return { brand_name, purpose, product_type, manufacturer_name, id };
};
